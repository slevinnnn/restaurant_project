/** @type {import('tailwindcss').Config} */
module.exports = {
  content: ["./templates/**/*.html", './static/styles/**/*.css'],
  theme: {
    extend: {},
  },
  plugins: [],
}
// This configuration file is for Tailwind CSS, specifying the content files to scan for class names,
// extending the default theme, and including any plugins if needed in the future.